// SCAFFOLD PLACEHOLDER — always-passing conformance case so `test:conformance`
// is GREEN on the empty scaffold. Slice S2 (schema) REPLACES this file with the
// real conformance corpus/tests under src/context/conformance/**/*.conformance.ts.
// S2 must NOT edit packages/urban/package.json (the test:conformance script is
// already pre-declared here by the scaffold).
import { test } from "node:test";
import assert from "node:assert/strict";

test("scaffold conformance placeholder — replaced by slice S2", () => {
  assert.equal(true, true);
});
