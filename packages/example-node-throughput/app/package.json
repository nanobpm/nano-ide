{
  "name": "node-throughput",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Camunda 8 / Nano throughput demo — async producer flood + JobWorker on the Nano-native @nanobpm/nano-sdk (a drop-in for the official @camunda8 JS SDK), run on the local Node runtime.",
  "scripts": {
    "prestart": "npm install --no-audit --no-fund --loglevel=error",
    "start": "node --experimental-strip-types --no-warnings main.ts",
    "pretypecheck": "npm install --no-audit --no-fund --loglevel=error",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nanobpm/nano-sdk": "^1.2.5"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6.0"
  }
}
