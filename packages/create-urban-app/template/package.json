{
  "name": "__APP_ID__",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "__APP_NAME__ — an Urban app.",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "check": "urban check",
    "gen": "urban gen",
    "gen:check": "urban gen --check",
    "start": "urban run",
    "dev": "urban dev",
    "deploy": "urban deploy",
    "test": "node --test --experimental-strip-types \"tests/**/*.test.ts\""
  },
  "dependencies": {
    "@nanobpm/urban": "^0.24.0"
  },
  "devDependencies": {
    "@nanobpm/urban-testkit": "^0.1.0"
  }
}
